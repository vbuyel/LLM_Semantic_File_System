[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (fast, no external deps)
    integration: Integration tests (may need DB/Kafka)
    system: System / end-to-end tests across services
    acceptance: User-facing acceptance tests
addopts = -v --tb=short
